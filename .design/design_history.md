# Design history

## 2025-03-25

- Streamlit header: compact hero (HTML flex, 32px logomark, smaller title, tagline stacked under title; less vertical gap before tabs). `ui/app.py`.
- Hero branding: full-viewport breakout + left align (not tied to centered content column). `ui/app.py`.
